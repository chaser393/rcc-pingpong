# Copy to docker.env and choose a private first-manager setup key.
# This is separate from your manager password (which only needs to be nonblank).
SETUP_KEY=
PONG_PORT=3000
PONG_BIND_IP=0.0.0.0
# Leave empty for direct LAN access. Set this when using your HTTPS domain.
PUBLIC_ORIGIN=
